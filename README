= pyexist =

This is a simple Python library for communicating with an eXist XML database
via its REST API.

= Dependencies =

* Python 2.5 or greater
* eXist DB [http://www.exist-db.org/] (not necessarily on the machine were
  this library is used)
